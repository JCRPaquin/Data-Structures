/**
 * Tries aren't actually a way to store strings. I just decided to leave them
 *   here because they're very heavily related to strings.
 *
 * Tries are essentially a form of map from strings to other types of values.
 *   They may outperform a hash map in certain scenarios.
 *
 * @author Jean-Claude Paquin
 **/

#ifndef DATA_STRUCTURES_STR_TRIE_H
#define DATA_STRUCTURES_STR_TRIE_H


class str_trie {

};


#endif //DATA_STRUCTURES_STR_TRIE_H
